{
  "name": "truffle-hardhat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "test-p": "hardhat test ./test/protocol.test.js",
    "compile": "hardhat compile",
    "create-set": "hardhat deploy --tags SetToken",
    "deploy": "hardhat run scripts/deploy.js --network fork",
    "build": "cd client && npm install && npm run build",
    "client": "cd client && yarn start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers",
    "@nomiclabs/hardhat-etherscan": "^2.1.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.6",
    "hardhat": "2.6.1",
    "hardhat-deploy": "^0.9.1",
    "prettier": "^2.2.1",
    "prettier-plugin-solidity": "^1.0.0-beta.8"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.1.0",
    "dotenv": "^8.2.0"
  }
}
